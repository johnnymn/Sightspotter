# Sightspotter

## Setup

We use CocoaPods to manage the external dependencies. The `Pods/` folder is
versioned, so you can clone the repo, open the `Sightspotter.xcworkspace` and
everything should work.
